[pytest]
# Restrict discovery to this project's own test suite. Without this,
# pytest's default recursive discovery also finds test_*.py files inside
# any repository indexed via the Streamlit app (cloned under
# data/repositories/<owner>_<repo>/), which can collide with this
# project's own `tests` package name and break collection entirely.
testpaths = tests
