"""Database layer: persistence adapters for SQLite, the graph store, and the vector store.

Infrastructure layer. Other layers depend on interfaces exposed here, not
on specific database engines.
"""
