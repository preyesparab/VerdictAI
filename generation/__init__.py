"""Generation layer: context assembly and LLM-based answer synthesis.

Consumes retrieval outputs (via `models/` schemas) only; has no direct
dependency on the database or retrieval implementation details.
"""
