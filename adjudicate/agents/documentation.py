"""Documentation agent (Phase 35 — stretch, not yet implemented).

Will generate/update documentation for a reviewed change once the Judge
(Phase 30) has rendered a verdict, using the same `ContextBundle`
convention as the Defender/Prosecutor.

Empty scaffold module (Phase 23). No prompt, schema, or LLM call exists
here yet — implementing this is explicitly out of scope for Phase 23.
"""

from __future__ import annotations
