"""Streamlit UI layer (Phase 18).

Everything in this package is a *consumer* of `pipeline.Pipeline` and the
backend's existing public APIs - nothing here is imported by any backend
module or by `pipeline.py`, and no backend module is modified to support
it.
"""
